{
  "name": "climbing-points-system",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy",
    "serve": "firebase serve",
    "build-deploy": "node build-and-deploy.js"
  },
  "dependencies": {
    "firebase": "^12.1.0",
    "pinia": "^2.1.7",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "firebase-tools": "^14.12.0",
    "vite": "^5.0.0"
  }
}
